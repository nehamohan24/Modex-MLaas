{
  "name": "mlaas-platform",
  "version": "1.0.0",
  "description": "Collaborative MLaaS Platform with Encrypted Data Training",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && python app.py",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "keywords": ["mlaas", "federated-learning", "encryption", "collaborative-ml"],
  "author": "MLaaS Platform",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
